# Managed by repository-settings/app
# https://github.com/repository-settings/app

repository:
  description: "A local inference engine for Apple Silicon, built around Rust and Metal."
  default_branch: main

  topics:
    - apple-silicon
    - inference
    - metal
    - mlx
